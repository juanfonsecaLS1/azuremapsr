% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hhtr.R
\name{get_azure_route}
\alias{get_azure_route}
\title{Get route directions from Azure Maps API}
\usage{
get_azure_route(
  origin,
  destination,
  API_key,
  waypoints = NULL,
  travel_mode = "car",
  api_version = "1.0"
)
}
\arguments{
\item{origin}{A numeric vector of c(latitude, longitude) for the starting point.}

\item{destination}{A numeric vector of c(latitude, longitude) for the ending point.}

\item{waypoints}{An optional list of waypoints, where each element is a numeric
vector c(latitude, longitude). You can have up to 150 waypoints.}

\item{travel_mode}{A string specifying the mode of travel. Can be "car", "truck",
"bicycle", or "walk". Defaults to "car".}

\item{api_version}{The version of the API to use. Defaults to "1.0".}

\item{subscription_key}{Your Azure Maps subscription key (string).}
}
\value{
A list containing the parsed JSON response from the API.
}
\description{
Get route directions from Azure Maps API
}
\examples{
\dontrun{
  # Replace with your actual key and coordinates
  my_key <- "YOUR_AZURE_MAPS_KEY"
  start_point <- c(47.6062, -122.3321) # Seattle
  end_point <- c(34.0522, -118.2437)   # Los Angeles

  route_data <- get_azure_route(start_point, end_point, my_key)

  # Print the distance and travel time
  summary <- route_data$routes$summary
  print(paste("Total distance:", summary$lengthInMeters, "meters"))
  print(paste("Estimated travel time:", summary$travelTimeInSeconds, "seconds"))
}
}
